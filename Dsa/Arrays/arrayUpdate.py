# update mewans changing the the value of an existing element
numbers = [10,20,30]
numbers [1] = 24
print(numbers)

#updating using loop
numbers = [1,2,3,4,5]
for i in range(len(numbers)):
    numbers[i] = numbers[i] *2
print(numbers)
